These steps is responsible to set the eas login and settings, not being used for now but 
it is imprtant to keep before moving this into chemisttasker production.

=========================
npm install -g eas-cli

eas login
npx expo login

npx eas credentials


Install/update deps:

npm install expo-notifications expo-device expo-constants (in frontend_mobile)
npm install -g eas-cli
Expo/EAS login:

npx expo login
eas login
EAS credentials (FCM v1 key upload):

npx eas credentials
(Android → development → Google Service Account → Manage Push (FCM V1) → Set up → Upload new key, selected chemisttasker-dd2ad-firebase-adminsdk-fbsvc-2747110cf0.json)
(Attempted and failed with old CLI): expo push:android:upload --api-key ./chemisttasker-dd2ad-firebase-adminsdk-fbsvc-2747110cf0.json
Builds:

eas build --profile development --platform android (dev client)
(Later rebuild with new FCM config): eas build --profile development --platform android
Running the app with dev client:

npx expo start --dev-client --clear (then scanned the “Development build” QR with the installed dev client APK)
Misc (auth/refresh):

npx expo whoami (optional to check login)
expo push:android:upload ... (deprecated/404 with old global CLI; replaced by npx eas credentials)
If you rerun on another machine: install deps, log into Expo/EAS, ensure app.json and google-services.json are in place, upload the FCM v1 key via npx eas credentials, rebuild with eas build --profile development --platform android, install APK, and run npx expo start --dev-client --clear to load the app.

when have we sat this mostafarg.chemisttasker and where?


We set com.mostafarg.chemisttasker when the EAS build prompt asked for the Android application id during the first dev build. After that, we saved it in frontend_mobile/app.json under expo.android.package so it persists for all builds.
************
npx eas build --profile production-apk --platform android --clear-cache
eas build:download --platform android --latest


eas build -p android --profile production
npx eas build -p ios --profile production --clear-cache

then this
npx eas submit -p ios --profile production

=========================









These are steps to debug and setup workflow before running expo


Here’s a complete and organized checklist to debug and restore your Django + Expo LAN 
setup step-by-step, including all the critical checks and commands like IP stack 
reset and firewall validation:

✅ Step-by-Step Debug Checklist
1️⃣ Start Django Backend
python manage.py runserver 0.0.0.0:8000


This ensures Django listens on all interfaces, including LAN.

2️⃣ Verify Python is Listening
Get-NetTCPConnection -LocalPort 8000


✅ You should see:

LocalAddress    : 0.0.0.0
State           : Listen

3️⃣ Check Your Laptop's IP Address
ipconfig


✅ Look for:

IPv4 Address. . . . . : 192.168.1.x


⚠️ Save this IP — it should be used in .env and Expo config.

4️⃣ Update .env.local in React Native
EXPO_PUBLIC_API_URL=http://<your-laptop-ip>:8000/api


Example:

EXPO_PUBLIC_API_URL=http://192.168.1.2:8000/api

5️⃣ Set Metro Packager to Use LAN IP
set REACT_NATIVE_PACKAGER_HOSTNAME=192.168.1.7

6️⃣ Start Expo
npx expo start --dev-client --lan --clear

7️⃣ Check if Metro is Listening
Get-NetTCPConnection -LocalPort 8081


✅ Expect to see:

LocalAddress : 0.0.0.0 OR your LAN IP
State        : Listen or Established

8️⃣ Ensure Your Mobile and Laptop Are on Same Network

Ping your phone from the laptop:

ping 192.168.1.X  # your phone's IP


Ping your laptop from your phone (use a ping app).

9️⃣ Check Windows Firewall

To view current status:

netsh advfirewall show allprofiles


✅ You want:

State : OFF


If not off, allow Node.js and Python through firewall manually or via:

netsh advfirewall firewall add rule name="Expo Metro 8081" dir=in action=allow protocol=TCP localport=8081
netsh advfirewall firewall add rule name="NodeJS" dir=in action=allow program="C:\Program Files\nodejs\node.exe" enable=yes
netsh advfirewall firewall add rule name="Python" dir=in action=allow program="C:\Path\To\python.exe" enable=yes

🔁 If All Fails: Reset Networking Stack
netsh int ip reset
netsh winsock reset
shutdown /r /t 0


🧠 This clears any corrupted network config and forces a reboot.

💣 Kill Conflicting Background Processes
taskkill /F /IM node.exe
taskkill /F /IM python.exe


🔁 Only do this if you're restarting your server/bundler.

💡 Tips

Always recheck ipconfig after reboot/router restart — your IP may change.

Don't bind Django to a specific IP unless you want to hardcode it.

Use --lan with Expo to avoid using tunnels unless necessary.

If you're debugging Expo Go — use development builds for --dev-client.











==================
Below the steps for setting up the node models once we fork it 
each project sets it own files in folder such as 

node_modules
dist
and a tgz file that serve the shared-core fucntionality
and a json back file, this is unique per each setup and we need to remove previous files 
and regenerate it once again

copy these lines:
"""
cd c:\code\chemisttasker\shared-core
rmdir /s /q node_modules
rmdir /s /q dist
del /f /q package-lock.json
del /f /q *.tgz

npm install
npm run build
npm pack


cd c:\code\chemisttasker\frontend_mobile
rmdir /s /q node_modules
rmdir /s /q .expo
del /f /q package-lock.json
del /f /q *.tgz
copy /Y ..\shared-core\chemisttasker-shared-core-1.0.0.tgz .\

npm install --include=dev

cd c:\code\chemisttasker\frontend_web
rmdir /s /q node_modules
rmdir /s /q dist
del /f /q package-lock.json

npm install


"""


Quick setup

cd shared-core
npm install
npm run build
npm pack
cd ..\frontend_web
npm install
cd ..\frontend_mobile
npm install




cd backend
venv\scripts\activate
python manage.py runserver 0.0.0.0:8000

cd backend
venv\scripts\activate
python manage.py qcluster

cd backend
stripe listen --events checkout.session.completed,invoice.paid,customer.subscription.deleted --forward-to localhost:8000/api/billing/webhook/

cd frontend_web
npm run dev:5173

cd frontend_web
npm run dev:5174

cd frontend_web
npm run dev:5175

cd frontend_mobile
set REACT_NATIVE_PACKAGER_HOSTNAME=192.168.1.7
npx expo start --dev-client --lan --clear
