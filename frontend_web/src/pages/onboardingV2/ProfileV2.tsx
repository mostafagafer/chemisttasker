import { Box, Typography } from "@mui/material";

export default function ProfileV2() {
  return (
    <Box>
      <Typography variant="h6" sx={{ mb: 2, fontWeight: 700 }}>Profile (V2)</Typography>
      <Typography variant="body2">
        Keep it as it is (placeholder). Later, we’ll map progress contributions here as well.
      </Typography>
    </Box>
  );
}
